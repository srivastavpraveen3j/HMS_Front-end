import { GenericHelperPipe } from './generic-helper.pipe';

describe('GenericHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new GenericHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
