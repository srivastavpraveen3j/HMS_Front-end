import { DateToISTPipe } from './date-to-ist.pipe';

describe('DateToISTPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToISTPipe();
    expect(pipe).toBeTruthy();
  });
});
